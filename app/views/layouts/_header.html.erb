<h2>WikiSite</h2>

<% if user_signed_in? %>
  <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
  <p>
    Welcome user # <%= current_user.id %> with an email <%= current_user.email %>
  </p>
<% else %>
  <%= link_to 'Log in', new_user_session_path %>
<% end %>
